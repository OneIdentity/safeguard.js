module.exports = {
    // The appliance host name or IP address
    'hostName': '',

    // The trusted root ca of the appliance
    'caFile': '',

    // The user name for password authentication
    'passwordUserName': '',

    // The password for password authentication
    'passwordPassword': '',

    // A .pem file for certificate authentication
    'userCertificateFile': '',

    // The corresponding .key file for certificate authentication
    'userCertificateKey': '',

    // The password for the .key file for certificate authentication
    'userCertificatePassphrase': '',

    // A .pfx file for external certificate authentication
    'externalUserCertificateFile': '',

    // The password for the .pfx file for external certificate authentication
    'externalCertificatePassphrase': '',

    // The root CA for external certificate authentication
    'externalCertificateCA': '',

    // The provider name or ID for external certificate authentication
    'externalCertificateProvider': '',

    // The user name for external password authentication
    'externalUserName': '',

    // The password for external password authentication
    'externalPassword': '',

    // The provider name or ID for external password authentication
    'externalProvider': '',

    // A .pem file for A2A communication
    'a2aCertificateFile': '',

    // The corresponding .key file for A2A communication
    'a2aKeyFile': '',

    // The password for the .key file for A2A communication
    'a2aKeyPassphrase': '',

    // The API Key for password retrieval via A2A
    'a2aPasswordApiKey': '',

    // The API Key for private key retrieval via A2A
    'a2aSshKeyApiKey': ''
}